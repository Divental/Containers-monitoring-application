version: "3.8"

services:
  my-app:
    image: devental/monitoring-container-app2
    container_name: my-python-container
    restart: always
    environment:
      - DOCKER_HOST=tcp://host.docker.internal:2375